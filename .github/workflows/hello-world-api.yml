name: hello-world-api

on:
  push:
    branches: [main]
    paths:
      - 'hello-world-api/**'
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version-file: hello-world-api/go.mod

      - name: Test
        working-directory: hello-world-api
        run: go test -race ./...

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: hello-world-api
          platforms: linux/arm64
          push: true
          tags: |
            ghcr.io/cujarrett/hello-world-api:latest
            ghcr.io/cujarrett/hello-world-api:sha-${{ github.sha }}
